extern func print_int(int) bool;
extern func print_string(string) bool;
package lab6 {
          var  x[100] int;
          var  y int;
          var  y bool = true;

     func main() int
        { var x  int ;
          var y int ;
            while (x+ 5/2 <= 2 + z - 5) 
             {
               if ( h - 2 >= 3 - 2)
                { read (x[100]);
                }
              else
                {
                 write (x[100] + 200);
                } // of else
            f(3+x[x[100]], bar+200, 20);
            return ;
            return (x+5+7);
            if ( x+10 > 10*20 ) 
                {  x = x!=10;}
            write (! ( (3 && 5) || (true &&  ! false)));
            }
       } // of main

      func f(x int,  y bool) void
            {
            } // of f
} // of package lab6
